# Backend configuration for Terraform state
#
# このファイルは S3 バックエンド（状態ファイルの保存先）の設定です。
# terraform init 時に -backend-config オプションで指定します:
#   terraform init -backend-config=backend.hcl
#
# ※ terraform.tfvars とは異なり、自動読み込みされません
#
# 使い方:
#   cp backend.hcl.example backend.hcl
#   # 値を編集

bucket  = "your-terraform-state-bucket"
key     = "stg/terraform.tfstate"
region  = "ap-northeast-1"
profile = "terraform"  # ~/.aws/config で設定したプロファイル名

# Optional: Enable state locking with DynamoDB
# dynamodb_table = "terraform-lock"

# Optional: Enable encryption (recommended)
# encrypt = true
